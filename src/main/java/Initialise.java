public class Initialise implements Runnable {

    // Method requests user inputs via Telegram inline buttons to initialise the Bot settings.
    public void run() {
        Bot botObject = new Bot();
        //code

    }
}
