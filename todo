ToDo:

- Class Bot > Method getBotToken > read token from local file.

- Include method to send bot a message.

- Include method to allow inline buttons.

- hand over order direction to market-maker and place order.





Terminal helpers:
bot details
  curl -s https://api.telegram.org/bot<token>/getMe

get messages
  curl -s https://api.telegram.org/bot<token>/getUpdates